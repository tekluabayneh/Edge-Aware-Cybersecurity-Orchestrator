services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
  
  client:
    build: ./client
    ports:
      - "3000:3000"

  agent:
    build: ./agent

  analyzer:
    build: ./analyzer
    environment:
      - LOG_LEVEL=info
