version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  agent:
    build: ./agent

  analyzer:
    build: ./analyzer
    ports:
      - "9090:9090"

  client:
    build: ./client
    ports:
      - "5173:5173"

